# 娱乐 Agent · 手机端 H5 · 产品架构文档

> 版本：v2.0 | 更新：2026-03-29 | Owner：Miles Zhang

---

## 1 项目定位

手机端作为遥控器，向车端 AI 大脑发送用户意图 JSON，车端推理后编排桌面卡片。

| 场景 | 用户行为 | 车端预期 |
|------|---------|---------|
| 娱乐需求响应 | 输入"不想听音乐了，我想听播客" | 喜马拉雅推荐卡移入堆栈顶 |
| 上车智能推荐 | 选择用户 + 行程 + 触发上车 | 根据 persona × trip × time 推荐最佳卡片 |
| AI 播客生成 | 发送内容链接 | 生成播客 → 播客卡移入堆栈顶 |

---

## 2 系统架构

```
┌─────────────┐          ┌──────────────────────┐          ┌──────────┐
│  手机端 H5   │←──WSS──→│   阿里云服务器         │←──WSS──→│  车端 AI  │
│  Netlify     │          │                      │          │  Android  │
└─────────────┘          │  relay.js (:9000)     │          └──────────┘
       │                 │  podcast-api.js(:9001)│
       │── POST ────────→│  Nginx (:8443 SSL)    │
       │  /api/podcast   │  audio/ (mp3 存储)    │
       │                 │  podcast-store.json   │
       │                 └──────────────────────┘
       │                          │
       │                          │── WSS + Header ──→ 豆包 API
       │                          │   (播客生成代理)
```

| 服务 | URL |
|------|-----|
| 手机端 H5 | https://stalwart-sunburst-2b6e9f.netlify.app/ |
| 中继 WSS | wss://zhangchang.duckdns.org:8443/ws?room=car_001 |
| 播客 API | https://zhangchang.duckdns.org:8443/api/podcast |
| 播客列表 | https://zhangchang.duckdns.org:8443/api/podcast/list |
| 健康检查 | https://zhangchang.duckdns.org:8443/health |

### 各端职责

| 端 | 负责 | 不负责 |
|----|------|-------|
| **手机端** | 用户切换、文字输入、播客触发、上车触发、JSON 发送 | 推理、渲染、播放 |
| **服务器** | 消息中继、播客生成代理、音频存储 | 业务决策 |
| **车端** | 接收 JSON → LLM 推理 → 卡片渲染 → 音频播放 → ACK | 用户输入 |

---

## 3 AI 播客链路

### 播客生成流程

```
手机输入 URL → POST /api/podcast
  │
  ├─ store 中已有？ → YES → 直接返回缓存（0 token，0 等待）
  │
  NO → 豆包 API 生成（2-10 分钟）
  │    → SSE 流式进度推送给手机端
  │    → PodcastEnd 拿到 cdn_url
  │    → 下载 mp3 到服务端持久存储
  │    → 写入 podcast-store.json
  │
  └→ 手机端组装 postcard JSON → 经中继发送到车端 → 车端播放
```

### 三阶段 UI 状态

| 阶段 | 触发 | 手机端显示 |
|------|------|-----------|
| 1 | 用户发出 URL | "链接已收到！正在生成播客..." |
| 2 | 豆包开始生成 | "正在生成第 N 段..." |
| 3 | 生成完成 | "生成完成！请在车端查看播客详情" |

缓存命中时直接跳到阶段 3（"播客已就绪！"）。

### 预设播客 vs 实时生成

| | 预设播客 | 实时生成 | 缓存命中 |
|--|--------|---------|---------|
| 触发 | 从预设列表选择 | 输入自定义 URL | 输入已有 URL |
| 等待 | 0s | 2-10 分钟 | 0s |
| Token 消耗 | 0 | ~15K-50K | 0 |
| JSON type | `source.type: "preset"` | `source.type: "url"` | `source.type: "url"` |

### 性能基线

| 指标 | 值 |
|------|-----|
| 生成速度 | 2.7× 实时（1 分钟播客只需 22s） |
| 首 token | avg 6s |
| 音频格式 | MP3, 96kbps, 24kHz mono |

---

## 4 六条交互链路

### A 切换用户

点击顶栏用户胶囊 → 底部弹窗选择 → 更新本地状态（不发 JSON）。

### B 文字输入 → content JSON

输入文字 → 发送 → 用户气泡 + content 反馈卡：
- 文案：「您的需求已收到！将在车端显示效果」
- 底部状态：发送中 → 已发送至车端 / 发送失败（含原因 + 重试按钮）

### C 触发上车 → recommend JSON

点击「模拟上车」→ 弹窗选行程 → 系统消息 + recommend 反馈卡：
- 文案：「用户上车了！需要推荐适合用户和当前场景的最佳内容」
- 标签行：用户标签 + 场景标签 + 时长标签

### D+E AI 播客 → postcard JSON

点击「AI播客」→ 弹窗选预设/输入 URL → 生成状态 + postcard 反馈卡：
- 标题：文章真实标题（服务端自动提取）
- 副标题：「两位AI主播为你深度解读这篇文章，涵盖核心观点和延伸信息」
- 原链接：可点击跳转
- 时长：`时长 mm:ss · MP3 · 96kbps`

### F 查看 JSON

点击任意卡片「查看JSON」→ 弹窗展示格式化 JSON。

---

## 5 播客内容存储

### 存储位置

```
/opt/relay/
├── podcast-store.json     # 播客索引
└── audio/                 # mp3 文件
```

### 数据模型

```json
{
  "id": "gen_1774767112003",
  "source_url": "https://mp.weixin.qq.com/s/xxx",
  "title": "小米在人工智能和机器人领域",
  "desc": "小米最近的AI技术进展...",
  "audio_file": "gen_1774767112003.mp3",
  "duration_sec": 682,
  "created_at": "2026-03-29T14:00:00Z",
  "source": "preset | generated"
}
```

### 去重机制

同一 `source_url` 只生成一次。后续请求直接返回缓存。

### 音频持久化

豆包 CDN URL 24h 过期。服务端在生成完成后立即下载 mp3 到本地，车端拿到的是服务端永久 URL。

---

## 6 部署架构

### 服务器（阿里云）

| 组件 | 端口 | 说明 |
|------|------|------|
| Nginx | 8443 (SSL) | Let's Encrypt 证书，反代所有服务 |
| relay.js | 9000 | WebSocket 房间广播 |
| podcast-api.js | 9001 | 播客生成 / 存储 / 音频服务 |
| PM2 | — | 进程守护 + 开机自启 |

### Nginx 路由

| 路径 | 目标 |
|------|------|
| `/ws` | relay.js（WebSocket 升级） |
| `/api/podcast` | podcast-api.js/generate（SSE） |
| `/api/podcast/list` | podcast-api.js/list |
| `/api/podcast/audio/` | podcast-api.js/audio/ |

### 部署命令

```bash
SSHPASS=密码 ./server/deploy.sh   # 只更新代码，不碰数据
```

---

## 7 演示 Checklist

| # | 步骤 | 预期 |
|---|------|------|
| 1 | 切换至用户A | 顶栏更新 |
| 2 | 输入"不想听音乐了，我想听播客" | content JSON → 车端推荐卡变化 |
| 3 | 模拟上车：早上通勤 30min | recommend JSON → 车端全量编排 |
| 4 | AI播客 → 选预设内容 | 0s → 车端播放 |
| 5 | AI播客 → 输入自定义 URL | 渐进状态 → 生成完成 → 车端播放 |
| 6 | 查看任意 JSON | 弹窗展示完整 JSON |

---

## 附录

### Persona

| ID | 标签 | 偏好 |
|----|------|------|
| `user_a` | 欧美流行重度发烧友 | 欧美流行、电子、Hip-hop |
| `user_b` | 喜欢听国语民谣、轻音乐 | 华语民谣、轻音乐、Acoustic |
| `user_c` | 喜欢听古典乐、播客 | 古典乐、知识播客、有声书 |

### 行程类型

| scene_id | 中文名 |
|----------|--------|
| `morning_commute` | 早上通勤 |
| `commute_home` | 下班回家 |
| `weekend_outing` | 周末出游 |
| `couple_time` | 情侣时光 |
| `kid_mode` | 儿童 |

### period 推导

| hour | period |
|------|--------|
| 5-8 | `early_morning` |
| 9-11 | `morning` |
| 12-16 | `afternoon` |
| 17-20 | `evening` |
| 21-4 | `late_night` |

### 设计 Token

| Token | 值 |
|-------|-----|
| 背景色 | `#F8F9FC` |
| 主文字色 | `#1A1A2E` |
| 品牌色 | `#4A6CF7` → `#7B5CFA` 渐变 |
| content 色 | `#4A6CF7` |
| postcard 色 | `#7B5CFA` |
| recommend 色 | `#22C55E` |
| 字体 | Source Sans 3 |
| 图标 | Lucide |

---

## 版本记录

### v1.0 (2026-03-28) — 初始架构设计

- 确定手机端 H5 + 车端 AI 的遥控器架构
- 定义三种 JSON 协议：content / recommend / postcard
- 定义六条交互链路（A~F）
- 完成豆包播客 API POC 验证（7 篇微信长文，共 93 分钟音频，全部成功）
- 性能基线：生成速度 2.7× 实时，首 token avg 6s，首段音频 avg 18s
- 豆包二进制协议 v3 移植验证（Python → 文档化关键坑点 7 条）
- UI 设计稿完成（10 屏交互稿，Pencil 格式）
- 设计 Token 定义 + 图标清单

### v1.1 (2026-03-28) — 对接协议

- 定义通用信封结构（type / session_id / timestamp / user_profile / payload）
- 定义 ACK 回传格式（status / session_id / message）
- 编写 curl 快速验证示例
- 定义 Persona 枚举（3 个）、行程类型枚举（5 个）、period 推导规则

### v1.3 (2026-03-29) — H5 开发完成

- Vite + Vanilla JS 单页对话式 UI（390×844）
- 六条交互链路全部实现
- 设计稿像素级还原（CSS 对齐 .pen 文件精确数值）
- 预设播客 7 首（3 persona × 4 首，复用 7 个音频文件）
- Mock 模式：未连接服务端时自动降级，2s 模拟 ACK
- JSON 查看弹窗：深色背景 + 语法高亮（状态机解析器）

### v1.5 (2026-03-29) — 通信层

- 方案评估：服务端中转 vs 局域网直连 vs 轻量中继 → 选择轻量中继
- relay.js 实现（< 60 行，WebSocket 房间广播）
- 阿里云部署：PM2 守护 + Nginx SSL 反代 + Let's Encrypt 证书
- DuckDNS 免费域名（zhangchang.duckdns.org）绕过 ICP 备案
- 端口 8443（非 443）绕过未备案域名拦截
- api.js 改造：自动连接 + 指数退避重连 + Mock 降级
- Netlify 部署 + GitHub 自动构建

### v1.7 (2026-03-29) — 豆包实时播客

- podcast-api.js：Python 二进制协议移植到 Node.js
- 浏览器 WebSocket 不支持自定义 Header → 服务端代理方案
- SSE 流式进度推送（phase / progress / meta / complete / error）
- 端到端测试通过：手机 → 服务端 → 豆包 → 服务端 → 手机 → 中继 → 车端 → ACK
- 实测：682s 播客 / 131s 生成 / 23 轮 / 3.8MB

### v2.0 (2026-03-29) — 内容存储 + UI 打磨

**内容存储架构**：
- podcast-store.json 存储所有播客（预设 + 用户生成）
- 同一 URL 去重（0 token，0 等待）
- mp3 下载到服务端持久存储（不依赖豆包 24h CDN）
- GET /api/podcast/list 返回完整播客库
- deploy.sh 安全部署脚本（只更新代码，自动备份 store）

**标题提取**：
- 微信文章服务端 fetch 返回"环境异常"（反爬）
- 方案：用豆包 Round 2 文案提取主题（去口语化前缀，截取 18 字）
- 标题优先级：网页抓取 > 豆包文案提取 > fallback "播客"

**UI 优化**：
- 用户头像颜色区分（A 紫 / B 琥珀 / C 绿）
- 卡片去掉重复状态（只保留底部）
- 统一失败文案 + 显示原因（"发送失败，车端未响应"）
- 重试按钮（蓝色醒目，只重发 JSON 不重新生成）
- 用户切换分割线 + 任务间距增大
- JSON 查看器配色修复（状态机解析器重写）
- 播客卡片增加原文链接（可点击跳转）
- 上车卡片文案简化 + 用户/场景标签分行显示
- 副标题统一为"两位AI主播为你深度解读这篇文章，涵盖核心观点和延伸信息"
